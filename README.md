# Connections-Game
Created a simple clone of the NYT game, Connections.
See original game at: https://www.nytimes.com/games/connections

## Starting the game
In a terminal, run `npm install`, followed by `npm start`.
The app should now be running in the browser at `localhost:3000`;
